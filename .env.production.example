# Copiar para .env.production (build/deploy Cloudflare) e .dev.vars se precisar em preview.
# Não commitar valores reais.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_ORG_SLUG=orange

# Opcional — só se alguma rota server usar service role
# SUPABASE_SERVICE_ROLE_KEY=
